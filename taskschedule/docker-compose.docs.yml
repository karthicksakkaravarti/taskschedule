services:
  docs:
    image: taskschedule_local_docs
    container_name: taskschedule_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./taskschedule:/app/taskschedule:z
    ports:
      - '9000:9000'
    command: /start-docs
